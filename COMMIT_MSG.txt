CI: Replace tauri-action with direct pnpm tauri build

The tauri-apps/tauri-action@v2 tag does not exist (the action
repo has no v2 release). Replace it with a direct shell command
so the Windows build can proceed.
