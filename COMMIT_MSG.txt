fix: remove unnecessary `mut` on stdout/stderr variables in run_npx_install

Fixes Rust compiler warnings `variable does not need to be mutable`
for `stdout` and `stderr` on lines 285-286 of lib.rs. These variables
are consumed by `.take()` which moves the value, so `mut` is not needed.
