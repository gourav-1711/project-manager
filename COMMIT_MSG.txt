CI: Add release workflow (v* tags → GitHub Release with Windows installer)

New .github/workflows/release.yml triggers on v* tags, builds the
Windows Tauri app, and publishes the MSI + NSIS installers as
GitHub Release assets with auto-generated release notes.
